import re
import random
from telegram import Update
from telegram.ext import Application, CommandHandler, ContextTypes

# ============ ВАШ ТОКЕН ============
TOKEN = "8599574987:AAFIZrNJYkqSCUNyzk_2f4XtDUmysbJwA9k"

# ============ ВЕСЬ ТЕКСТ ИЗ ФАЙЛА (скопирован вручную) ============
FULL_TEXT = """
## Тема 1. Множества точек пространства Rm.

1.1. окрестности точки пространства Rm;
1.2. шаровой окрестности точки пространства Rm;
1.3. прямоугольной окрестности точки пространства Rm;
1.4. внутренней точки множества D точек пространства Rm;
1.5. изолированной точки множества D точек пространства Rm;
1.6. граничной точки множества D точек пространства Rm;
1.7. границы множества D точек пространства Rm;
1.8. открытого множества точек пространства Rm;
1.9. замкнутого множества точек пространства Rm;
1.10. предельной точки множества D точек пространства Rm;
1.11. непрерывной кривой в пространстве Rm;
1.12. связного множества точек пространства Rm.

2.1. Докажите, что любая внутренняя точка множества точек пространства Rm является его предельной точкой.
2.2. Докажите, что любая граничная точка множества точек пространства Rm является либо предельной точкой, либо изолированной точкой этого множества.
2.3. Докажите, что любая изолированная точка множества точек пространства Rm является граничной точкой этого множества.
2.4. Докажите, что множество точек пространства Rm, содержащее хотя бы одну свою граничную точку, не является открытым.
2.5. Докажите, что открытое множество не содержит ни одной своей граничной точки.
2.6. Приведите пример непустого множества точек на плоскости, которое не имеет внутренних точек.
2.7. Приведите пример непустого множества точек на плоскости, все точки которого являются граничными точками этого множества.
2.8. Приведите пример непустого множества точек на плоскости, которое совпадает со своей границей.
2.9. Приведите пример непустого множества точек на плоскости, все точки которого являются предельными точками этого множества.
2.10. Приведите пример множества точек на плоскости, каждая граничная точка которого является его предельной точкой.
2.11. Приведите пример множества точек на плоскости, для которого ни одна граничная точка не является его предельной точкой.
2.12. Приведите пример непустого множества точек на плоскости, для которого множество всех предельных точек не совпадает с множеством всех граничных точек.
2.13. Приведите пример непустого замкнутого множества точек на плоскости, которое не имеет ни одной предельной точки.
2.14. Найдите все граничные точки множества точек на плоскости {(x,y): x²+y²<1}.
2.15. Найдите все предельные точки множества точек на плоскости {(x,y): x²+y²<1}.
2.16. Найдите все граничные точки множества точек на плоскости {(x,y): x²+y²=1}.
2.17. Докажите, что объединение любого числа открытых множеств является открытым множеством.
2.18. Докажите, что пересечение конечного числа открытых множеств является открытым множеством.
2.19. Докажите, что объединение конечного числа замкнутых множеств является замкнутым множеством.
2.20. Докажите, что пересечение любого числа замкнутых множеств является замкнутым множеством.
2.21. Докажите, что непустое множество точек на плоскости, состоящее из конечного числа точек, совпадает со своей границей.
2.22. Докажите, что сфера в пространстве Rm является замкнутым множеством.

## Тема 2. Последовательности точек пространства Rm.

1.1. ограниченной последовательности точек пространства Rm;
1.2. неограниченной последовательности точек пространства Rm;
1.3. предела последовательности точек пространства Rm;
1.4. сходящейся последовательности точек пространства Rm;
1.5. предельной точки последовательности точек пространства Rm;
1.6. фундаментальной последовательности точек пространства Rm.

2.1. Сформулируйте и докажите теорему о критерии Коши сходимости последовательности точек пространства Rm.
2.2. Сформулируйте теорему Больцано-Вейерштрасса для последовательности точек на плоскости.
2.3. Докажите, что любая ограниченная последовательность точек на плоскости имеет по крайней мере одну предельную точку.
2.4. Докажите, что если последовательность точек {M_n(x_n,y_n)} на плоскости является сходящейся, то числовые последовательности {x_n} и {y_n} являются сходящимися.
2.5. Докажите, что если числовые последовательности {x_n} и {y_n} являются сходящимися, то последовательность точек {M_n(x_n,y_n)} на плоскости является сходящейся.
2.6. Докажите, что любая сходящаяся последовательность точек пространства Rm является ограниченной.
2.7. Найдите предел последовательности точек на плоскости: а) (cos(π/n), sin(π/n)); b) ((1+1/n)^n, n^(1/n)).
2.8. Найдите все предельные точки последовательности точек на плоскости (x_n,y_n) = (cos(πn/3), sin(πn/3)).

## Тема 3. Функции, предел, непрерывность.

1.1. ограниченной сверху (снизу) функции u(M), заданной на множестве D точек пространства Rm;
1.2. неограниченной сверху (снизу) функции u(M), заданной на множестве D;
1.3. точной верхней (нижней) грани функции u(M);
1.4. предела функции u(M) в точке M0 "по Коши";
1.5. предела функции u(M) в точке M0 "по Гейне";
1.6. предела функции u(M) в бесконечно удаленной точке "по Гейне";
1.7. предела функции u(M) в бесконечно удаленной точке "по Коши";
1.8. функции u(x,y), непрерывной по переменной x в точке M0(x0,y0);
1.9. функции u(x,y), непрерывной по совокупности переменных в точке M0(x0,y0);
1.10. повторного предела функции u(x,y) в точке M0(x0,y0).

2.1. Сформулируйте теорему о критерии Коши существования предела функции в точке.
2.2. Сформулируйте теорему о непрерывности суммы двух функций нескольких переменных.
2.3. Сформулируйте теорему о непрерывности произведения двух функций нескольких переменных.
2.4. Сформулируйте теорему о непрерывности частного от деления двух функций.
2.5. Сформулируйте теорему об устойчивости знака непрерывной функции.
2.6. Сформулируйте первую теорему Вейерштрасса для функции нескольких переменных.
2.7. Сформулируйте вторую теорему Вейерштрасса для функции нескольких переменных.
2.8. Сформулируйте теорему о непрерывности сложной функции нескольких переменных.
2.9. Докажите теорему о пределе суммы двух функций нескольких переменных.
2.10. Докажите теорему о непрерывности суммы двух функций нескольких переменных.
2.11. Докажите теорему об устойчивости знака непрерывной функции двух переменных.
2.12. Докажите первую теорему Вейерштрасса для функции двух переменных.
2.13. Приведите пример функции, ограниченной сверху и неограниченной снизу на множестве {(x,y): x²+y²≤1}.
2.14. Приведите пример функции двух переменных, которая непрерывна и ограничена на множестве {(x,y): x²+y²<1}, но не достигает на этом множестве своей точной верхней грани.
2.15. Найдите предел функции u(x,y) при M(x,y)→(0,0): u(x,y)=x²y²/(x⁴+y⁴); u(x,y)=2xy/(x²+y²).
2.16. Найдите предел функции u(x,y) при M(x,y)→∞: u(x,y)=(x²+y²)/(x⁴+y⁴); u(x,y)=xy sin(1/(x²+y²)).
2.17. Исследуйте функцию на непрерывность: u(x,y)=x²y/(x²+y²) при x²+y²≠0, u(0,0)=0.

## Тема 4. Дифференцируемые функции.

1.1. функции f(x1,...,xm), дифференцируемой в точке M;
1.2. частной производной функции f(x1,...,xm) по переменной xk;
1.3. первого дифференциала функции нескольких переменных;
1.4. касательной плоскости к графику функции z=f(x,y);
1.5. второго дифференциала функции f(x1,...,xm);
1.6. градиента функции f(x,y,z) в точке M(x0,y0,z0);
1.7. производной по направлению l=(cosα,cosβ,cosγ).

2.1. Сформулируйте теорему о необходимых условиях дифференцируемости.
2.2. Сформулируйте теорему о достаточных условиях дифференцируемости.
2.3. Сформулируйте теорему о достаточных условиях равенства fxy=fyx.
2.4. Сформулируйте теорему о касательной плоскости к графику функции двух переменных.
2.5. Сформулируйте теорему о дифференцируемости сложной функции.
2.6. Запишите выражение производной функции по направлению через градиент.
2.7. Запишите формулу Лагранжа конечных приращений для функции нескольких переменных.
2.8. Запишите выражение для второго дифференциала функции нескольких переменных.
2.9. Сформулируйте теорему о формуле Тейлора с остаточным членом в форме Пеано.
2.10. Докажите теорему о непрерывности дифференцируемой функции.
2.11. Докажите теорему о необходимых условиях дифференцируемости.
2.12. Докажите теорему о достаточных условиях дифференцируемости.
2.13. Докажите теорему о касательной плоскости к графику функции.
2.14. Докажите, что производная по направлению равна скалярному произведению направления и градиента.
2.15. Приведите пример функции, непрерывной в точке, но не дифференцируемой.
2.16. Приведите пример функции, у которой есть частные производные в точке, но она не дифференцируема.
2.17. Что означает "инвариантность формы первого дифференциала"?
2.18. Исследуйте функцию на дифференцируемость в точке (0;0): u(x,y)=xy; u(x,y)=x²y³; u(x,y)=xy·|xy|; u(x,y)=x³+y³.
2.19. Запишите формулу Тейлора порядка n с центром в точке M0 для функции u(x,y)=e^x sin y, M0(0;0), n=3.

## Тема 5. Локальный экстремум.

1.1. точки локального экстремума функции нескольких переменных;
1.2. положительно определённой квадратичной формы;
1.3. знакопеременной квадратичной формы.

2.1. Сформулируйте теорему о необходимых условиях локального экстремума.
2.2. Сформулируйте теорему о достаточных условиях локального экстремума.
2.3. Докажите теорему о необходимых условиях локального экстремума.
2.4. Докажите теорему о достаточных условиях локального экстремума.
2.5. Докажите, что сумма функций, имеющих локальный минимум, также имеет локальный минимум.
2.6. Приведите пример функций с локальным минимумом, произведение которых имеет максимум.
2.7. Приведите пример функций с локальным минимумом, произведение которых не имеет экстремума.
2.8. Приведите пример функции, имеющей локальный экстремум, у которой не существует частной производной.
2.9. Приведите пример функции, удовлетворяющей du(0;0)=0, но не имеющей экстремума.
2.10. Найдите все точки локального экстремума функций: u=x²+xy+y²; u=x³+y³-3xy.
2.11. Исследуйте наличие экстремума в точке (0;0): u=x³y³; u=(x-y²)²; u=|x|+|y|; u=(y-sinx)².
2.12. Исследуйте точку M(π/2;0;1) на экстремум функции u=x cos y+z cos x.

## Тема 6. Неявные функции.

1.1. функции y=f(x), заданной неявно уравнением F(x,y)=0;
1.2. функции z=f(x,y), заданной неявно уравнением F(x,y,z)=0;
1.3. зависимости функций f1(x1,...,xn),..., fk(x1,...,xn);
1.4. независимости функций.

2.1. Сформулируйте теорему о существовании и непрерывности неявной функции y=f(x).
2.2. Сформулируйте теорему о дифференцируемости неявной функции y=f(x).
2.3. Сформулируйте теорему о существовании и непрерывности неявной функции z=f(x,y).
2.4. Сформулируйте теорему о существовании и дифференцируемости функций y=f(x), z=g(x), заданных системой.
2.5. Сформулируйте теорему о достаточных условиях независимости функций.
2.6. Докажите теорему о существовании и непрерывности неявной функции y=f(x).
2.7. Докажите теорему о дифференцируемости неявной функции y=f(x).
2.8. Напишите уравнение нормали и касательной к кривой F(x,y)=0.
2.9. Найдите первый дифференциал функции y=f(x), заданной неявно: x⁴+y³-2x²y=0.
2.10. Найдите частные производные и первый дифференциал функции z=z(x,y): xyz=x²+y²+z².
2.11. Найдите первый и второй дифференциалы функций y(x) и z(x), заданных системой {x+2y+3z=0, x²+y²+z²=1}.

## Тема 7. Условный экстремум.

1.1. экстремума функции u(x,y) с условием связи f(x,y)=0;
1.2. экстремума функции u(x,y,z) с условием связи f(x,y,z)=0;
1.3. экстремума функции u(x,y,z) с двумя условиями связи.

2.1. Сформулируйте теорему о необходимых условиях условного экстремума в форме Лагранжа.
2.2. Сформулируйте теорему о достаточных условиях условного экстремума в форме Лагранжа.
2.3. Докажите теорему о необходимых условиях условного экстремума.
2.4. Докажите теорему о достаточных условиях условного экстремума.
2.5. Используя метод Лагранжа, найдите точки возможного экстремума: u=x²+y² при x+y=2; u=xy при x+y=2; u=x+y при x²+y²=2; u=xy при x²+y²=2; u=x+y+z при xyz=1; u=xyz при x+y+z=3; u=x²+y²+z² при x+y+z=3.
2.6. Используя метод Лагранжа, найдите точки возможного экстремума: u=xyz при x²+y²+z²=1, x+y+z=0.

## Тема 8. Кратные интегралы.

1.1. квадрируемой плоской фигуры;
1.2. площади плоской фигуры;
1.3. интегральной суммы для двойного интеграла;
1.4. диаметра ограниченного множества G;
1.5. предела интегральных сумм при стремлении диаметра разбиения к нулю.

2.1. Сформулируйте теорему о необходимом и достаточном условии квадрируемости.
2.2. Сформулируйте теорему о площади криволинейной трапеции.
2.3. Сформулируйте теорему о сведении двойного интеграла к повторному.
2.4. Сформулируйте теорему о формуле замены переменных для двойного интеграла.
2.5. Сформулируйте теорему о среднем значении для двойного интеграла.
2.6. Запишите выражение для площади фигуры в декартовых и полярных координатах.
2.7. Запишите формулы для массы и координат центра тяжести пластины.
2.8. Запишите формулы для моментов инерции пластины.
2.9. Докажите теорему о площади криволинейной трапеции.
2.10. Докажите теорему о сведении двойного интеграла к повторному.
2.11. Докажите теорему об интегрируемости непрерывной функции.
2.12. Измените порядок интегрирования в повторных интегралах.
2.13. Сведите двойной интеграл к повторному двумя способами.
2.14. Вычислите двойной интеграл, перейдя в полярные координаты.
2.15. Вычислите массу, статические моменты и моменты инерции фигуры.
2.16. С помощью тройного интеграла вычислите объём тела.

## Тема 9. Криволинейные интегралы.

1.1. спрямляемой кривой;
1.2. длины дуги кривой;
1.3. гладкой кривой;
1.4. интегральной суммы для криволинейного интеграла I рода;
1.5. криволинейного интеграла I рода;
1.6. интегральной суммы для криволинейного интеграла II рода.

2.1. Сформулируйте теорему о вычислении криволинейного интеграла I рода.
2.2. Сформулируйте теорему о вычислении криволинейного интеграла II рода.
2.3. Запишите формулу для вычисления длины дуги кривой.
2.4. Запишите формулу для вычисления массы кривой через криволинейный интеграл.
2.5. Сформулируйте теорему о формуле Грина.
2.6. Сформулируйте теорему об условиях независимости криволинейного интеграла от пути.
2.7. Докажите теорему о формуле Грина.
2.8. Докажите теорему об условиях независимости от пути.
2.9. Вычислите криволинейные интегралы I рода.
2.10. Вычислите криволинейные интегралы II рода.
2.11. Вычислите длину кривой.
2.12. Вычислите массу кривой.
2.13. Вычислите работу силы.
2.14. С помощью формулы Грина вычислите интеграл.
2.15. С помощью криволинейного интеграла найдите площадь области.

## Тема 10. Поверхностные интегралы.

1.1. площади поверхности;
1.2. нормали к поверхности;
1.3. поверхностного интеграла первого рода;
1.4. двусторонней поверхности;
1.5. поверхностного интеграла второго рода.

2.1. Запишите формулу площади поверхности, заданной уравнением z=h(x,y).
2.2. Запишите формулу для вычисления поверхностного интеграла I рода.
2.3. Запишите формулу для вычисления поверхностного интеграла II рода.
2.4. Найдите вектор нормали и уравнение касательной плоскости к поверхности.
2.5. Найдите площадь поверхности с помощью поверхностного интеграла.
2.6. Вычислите поверхностные интегралы I и II рода.

## Тема 11. Кривые на плоскости.

1.1. того, что две кривые на плоскости касаются в данной точке;
1.2. порядка касания плоских кривых;
1.3. особой точки плоской кривой;
1.4. огибающей однопараметрического семейства кривых;
1.5. кривизны плоской кривой.

2.1. Сформулируйте теорему о необходимых и достаточных условиях порядка касания.
2.2. Сформулируйте теорему о необходимых условиях огибающей.
2.3. Запишите формулу для вычисления кривизны плоской кривой.
2.4. Запишите формулу для радиуса кривизны.
2.5. Найдите огибающую однопараметрического семейства кривых.
2.6. Найдите радиус кривизны кривой.
"""

# ============ ПАРСИНГ МАТЕРИАЛОВ ============

def parse_topics_and_items(text: str):
    """Парсит темы и связанные с ними материалы"""
    lines = text.split('\n')
    topics = []
    current_topic = None
    current_items = []
    
    for line in lines:
        line = line.strip()
        if not line:
            continue
        
        # Ищем заголовки тем (## Тема X. ...)
        topic_match = re.match(r'^##\s*Тема\s+(\d+)\.\s*(.*)', line)
        if topic_match:
            # Сохраняем предыдущую тему
            if current_topic and current_items:
                topics.append({
                    'number': current_topic['number'],
                    'name': current_topic['name'],
                    'items': current_items.copy()
                })
            # Начинаем новую тему
            current_topic = {
                'number': topic_match.group(1),
                'name': topic_match.group(2)
            }
            current_items = []
            continue
        
        # Ищем нумерованные пункты (1.1, 2.1 и т.д.)
        item_match = re.match(r'^(\d+(?:\.\d+)*)\.\s+(.*)', line)
        if item_match and current_topic:
            current_items.append({
                'number': item_match.group(1),
                'text': item_match.group(2)
            })
    
    # Добавляем последнюю тему
    if current_topic and current_items:
        topics.append({
            'number': current_topic['number'],
            'name': current_topic['name'],
            'items': current_items.copy()
        })
    
    return topics

def classify_item(item: dict) -> str:
    """Определяет тип пункта (определение, теорема, задача)"""
    num = item['number']
    text = item['text'].lower()
    
    # Определения (1.x)
    if num.startswith('1.'):
        return 'definition'
    
    # Теоремы (2.x с ключевыми словами)
    if num.startswith('2.'):
        theorem_keywords = ['теорем', 'докажите', 'сформулируйте', 'доказательство']
        for kw in theorem_keywords:
            if kw in text:
                return 'theorem'
        return 'problem'
    
    return 'problem'

# ============ ЗАГРУЗКА ДАННЫХ ============

print("=" * 50)
print("🤖 Бот по математическому анализу")
print("=" * 50)

topics = parse_topics_and_items(FULL_TEXT)

# Собираем все материалы по типам
all_definitions = []
all_theorems = []
all_problems = []
all_items_with_topic = []  # (тема, item)

for topic in topics:
    for item in topic['items']:
        item_type = classify_item(item)
        all_items_with_topic.append((topic, item))
        
        if item_type == 'definition':
            all_definitions.append((topic, item))
        elif item_type == 'theorem':
            all_theorems.append((topic, item))
        else:
            all_problems.append((topic, item))

print(f"📚 Найдено тем: {len(topics)}")
print(f"📖 Определений: {len(all_definitions)}")
print(f"📐 Теорем: {len(all_theorems)}")
print(f"❓ Вопросов/задач: {len(all_problems)}")
print(f"📚 Всего материалов: {len(all_items_with_topic)}")
print("=" * 50)
print("✅ Бот готов к работе!")
print("=" * 50)

# ============ КОМАНДЫ БОТА ============

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "📚 **Привет! Я бот для подготовки по математическому анализу-2**\n\n"
        "Я умею выдавать случайные материалы:\n\n"
        "📖 `/def` — случайное определение\n"
        "📐 `/th` — случайную теорему (с доказательством)\n"
        "❓ `/q` — случайный вопрос/задачу\n"
        "🎲 `/rand` — совершенно случайный материал\n"
        "📚 `/topic` — случайная тема\n"
        "📊 `/stats` — статистику\n\n"
        "Удачи в подготовке! 🍀",
        parse_mode="Markdown"
    )

async def random_definition(update: Update, context: ContextTypes.DEFAULT_TYPE):
    if not all_definitions:
        await update.message.reply_text("❌ Определения не найдены")
        return
    topic, item = random.choice(all_definitions)
    await update.message.reply_text(
        f"📖 **Определение** (Тема {topic['number']}: {topic['name']})\n"
        f"п. {item['number']}:\n\n{item['text']}",
        parse_mode="Markdown"
    )

async def random_theorem(update: Update, context: ContextTypes.DEFAULT_TYPE):
    if not all_theorems:
        await update.message.reply_text("❌ Теоремы не найдены")
        return
    topic, item = random.choice(all_theorems)
    await update.message.reply_text(
        f"📐 **Теорема** (Тема {topic['number']}: {topic['name']})\n"
        f"п. {item['number']}:\n\n{item['text']}",
        parse_mode="Markdown"
    )

async def random_problem(update: Update, context: ContextTypes.DEFAULT_TYPE):
    if not all_problems:
        await update.message.reply_text("❌ Вопросы/задачи не найдены")
        return
    topic, item = random.choice(all_problems)
    await update.message.reply_text(
        f"❓ **Вопрос/Задача** (Тема {topic['number']}: {topic['name']})\n"
        f"п. {item['number']}:\n\n{item['text']}",
        parse_mode="Markdown"
    )

async def random_any(update: Update, context: ContextTypes.DEFAULT_TYPE):
    if not all_items_with_topic:
        await update.message.reply_text("❌ Материалы не найдены")
        return
    topic, item = random.choice(all_items_with_topic)
    item_type = classify_item(item)
    emoji = {'definition': '📖', 'theorem': '📐', 'problem': '❓'}.get(item_type, '📌')
    type_name = {'definition': 'Определение', 'theorem': 'Теорема', 'problem': 'Вопрос/Задача'}.get(item_type, 'Материал')
    
    await update.message.reply_text(
        f"{emoji} **{type_name}** (Тема {topic['number']}: {topic['name']})\n"
        f"п. {item['number']}:\n\n{item['text']}",
        parse_mode="Markdown"
    )

async def random_topic(update: Update, context: ContextTypes.DEFAULT_TYPE):
    if not topics:
        await update.message.reply_text("❌ Темы не найдены")
        return
    topic = random.choice(topics)
    items_text = "\n".join([f"  • {item['number']}. {item['text'][:80]}..." for item in topic['items'][:5]])
    if len(topic['items']) > 5:
        items_text += f"\n  ... и ещё {len(topic['items'])-5} материалов"
    
    await update.message.reply_text(
        f"📚 **Тема {topic['number']}: {topic['name']}**\n\n"
        f"Материалов в теме: {len(topic['items'])}\n\n"
        f"Примеры:\n{items_text}",
        parse_mode="Markdown"
    )

async def stats(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "📊 **Статистика базы материалов:**\n\n"
        f"📚 Тем: {len(topics)}\n"
        f"📖 Определений: {len(all_definitions)}\n"
        f"📐 Теорем: {len(all_theorems)}\n"
        f"❓ Вопросов/задач: {len(all_problems)}\n"
        f"━━━━━━━━━━━━━━━━\n"
        f"📚 **Всего материалов:** {len(all_items_with_topic)}",
        parse_mode="Markdown"
    )

# ============ ЗАПУСК ============

def main():
    app = Application.builder().token(TOKEN).build()
    
    app.add_handler(CommandHandler("start", start))
    app.add_handler(CommandHandler("def", random_definition))
    app.add_handler(CommandHandler("th", random_theorem))
    app.add_handler(CommandHandler("q", random_problem))
    app.add_handler(CommandHandler("rand", random_any))
    app.add_handler(CommandHandler("topic", random_topic))
    app.add_handler(CommandHandler("stats", stats))
    
    # Короткие команды
    app.add_handler(CommandHandler("d", random_definition))
    app.add_handler(CommandHandler("t", random_theorem))
    app.add_handler(CommandHandler("qq", random_problem))
    app.add_handler(CommandHandler("r", random_any))
    app.add_handler(CommandHandler("tm", random_topic))
    
    print("\n🚀 Бот запущен и работает!")
    print("📱 Команды:")
    print("   /def  - определение")
    print("   /th   - теорема")
    print("   /q    - вопрос/задача")
    print("   /rand - случайный материал")
    print("   /topic - случайная тема")
    print("   /stats - статистика")
    print("\n⏹️ Для остановки нажмите Ctrl+C\n")
    
    app.run_polling()

if __name__ == "__main__":
    main()        return ""
    except Exception as e:
        print(f"❌ Ошибка при чтении PDF: {e}")
        return ""

def load_text() -> str:
    """Загружает текст из PDF-файла"""
    if not os.path.exists(PDF_FILE):
        print(f"❌ Файл {PDF_FILE} не найден!")
        print(f"📁 Текущая папка: {os.getcwd()}")
        print("Поместите PDF-файл в ту же папку, что и бот")
        return ""
    
    print(f"📄 Читаем PDF: {PDF_FILE}")
    text = extract_text_from_pdf(PDF_FILE)
    
    if text:
        print(f"✅ Загружено {len(text)} символов из PDF")
    else:
        print("❌ Не удалось извлечь текст из PDF")
    
    return text

# ============ ОЧИСТКА ТЕКСТА ============

def clean_text(text: str) -> str:
    """Очищает текст от битых символов и нормализует спецсимволы"""
    # Удаляем битые символы (оставляем только печатные)
    cleaned = re.sub(r'[^\x20-\x7E\x0A\x0D\u0400-\u04FF\u2000-\u206F\u2200-\u22FF\u25A0-\u25FF]', ' ', text)
    
    # Заменяем множественные пробелы на один
    cleaned = re.sub(r'\s+', ' ', cleaned)
    
    # Восстанавливаем часто встречающиеся математические символы
    replacements = {
        '∫': '∫',
        '∬': '∬',
        '∭': '∭',
        '∮': '∮',
        '∑': '∑',
        '∏': '∏',
        '∂': '∂',
        'Δ': 'Δ',
        '∇': '∇',
        '√': '√',
        '∛': '∛',
        '∜': '∜',
        '∞': '∞',
        '≠': '≠',
        '≤': '≤',
        '≥': '≥',
        '≈': '≈',
        'π': 'π',
        'α': 'α',
        'β': 'β',
        'γ': 'γ',
        'θ': 'θ',
        'λ': 'λ',
        'μ': 'μ',
        'ρ': 'ρ',
        'σ': 'σ',
        'τ': 'τ',
        'φ': 'φ',
        'ψ': 'ψ',
        'ω': 'ω',
        '→': '→',
        '⇒': '⇒',
        '⇔': '⇔',
        '·': '·',
        '×': '×',
        '÷': '÷',
        '±': '±',
        '∓': '∓',
    }
    
    # Также можно добавить замену для LaTeX-подобных команд
    # (оставляем как есть, они будут читабельны)
    
    return cleaned

# ============ ПАРСИНГ ВСЕХ ЭЛЕМЕНТОВ ============

def parse_all_items(text: str):
    """Парсит все нумерованные элементы и возвращает список с номерами и текстом"""
    lines = text.split('\n')
    items = []
    current_item = []
    current_number = ""
    
    for line in lines:
        line = line.strip()
        if not line:
            continue
        
        # Ищем номера вида 1.1, 2.1, 3.1, 4.1.1 и т.д.
        match = re.match(r'^(\d+(?:\.\d+)+)\.?\s*(.*)', line)
        
        if match:
            # Сохраняем предыдущий
            if current_item and current_number:
                items.append({
                    'number': current_number,
                    'text': ' '.join(current_item)
                })
            # Начинаем новый
            current_number = match.group(1)
            current_item = [match.group(2)] if match.group(2) else []
        elif current_item:
            current_item.append(line)
    
    # Добавляем последний
    if current_item and current_number:
        items.append({
            'number': current_number,
            'text': ' '.join(current_item)
        })
    
    return items

def classify_item(item: dict) -> str:
    """Классифицирует элемент на основе номера и текста"""
    number = item['number']
    text = item['text'].lower()
    
    # Определения (1.x)
    if re.match(r'^1\.\d+', number):
        return 'definition'
    
    # Задачи (3.x, 4.x, 5.x)
    if re.match(r'^[3-5]\.', number):
        return 'problem'
    
    # Для 2.x — смотрим на ключевые слова
    if re.match(r'^2\.\d+', number):
        # Ключевые слова для теорем
        theorem_keywords = ['теорем', 'докажите', 'сформулируйте и докажите', 'доказательство']
        for keyword in theorem_keywords:
            if keyword in text:
                return 'theorem'
        # Остальное — задачи
        return 'problem'
    
    return 'other'

def get_type_emoji(item_type: str) -> str:
    """Возвращает эмодзи для типа"""
    emojis = {
        'definition': '📖',
        'theorem': '📐',
        'problem': '❓',
        'other': '📌'
    }
    return emojis.get(item_type, '📌')

def get_type_name(item_type: str) -> str:
    """Возвращает русское название типа"""
    names = {
        'definition': 'Определение',
        'theorem': 'Теорема (с доказательством)',
        'problem': 'Вопрос/Задача',
        'other': 'Материал'
    }
    return names.get(item_type, 'Материал')

# ============ ЗАГРУЗКА ДАННЫХ ============

print("=" * 50)
print("🤖 Бот по математическому анализу")
print("=" * 50)

# Загружаем текст из PDF
raw_text = load_text()

if not raw_text:
    print("❌ Не удалось загрузить материалы. Бот не запустится.")
    print("Альтернатива: создайте файл questions.txt с текстом вопросов")
    exit(1)

# Очищаем текст от битых символов
raw_text = clean_text(raw_text)

# Парсим все элементы
all_items = parse_all_items(raw_text)

if not all_items:
    print("❌ Не удалось распарсить материалы из PDF.")
    print("Возможно, PDF имеет нестандартную структуру.")
    print("Попробуйте сохранить PDF как текстовый файл и использовать его.")
    exit(1)

# Классифицируем элементы
definitions = []
theorems = []
problems = []

for item in all_items:
    item_type = classify_item(item)
    if item_type == 'definition':
        definitions.append(item)
    elif item_type == 'theorem':
        theorems.append(item)
    else:
        problems.append(item)

print("-" * 50)
print(f"📖 Определений (1.x): {len(definitions)}")
print(f"📐 Теорем (2.x с 'докажите'): {len(theorems)}")
print(f"❓ Вопросов/задач (3.x,4.x,5.x и остальные 2.x): {len(problems)}")
print(f"📚 Всего материалов: {len(all_items)}")
print("=" * 50)
print("✅ Бот готов к работе!")
print("=" * 50)

# ============ КОМАНДЫ БОТА ============

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "📚 **Привет! Я бот для подготовки по математическому анализу**\n\n"
        "Я умею выдавать случайные материалы разных типов:\n\n"
        "📖 `/def` — случайное определение\n"
        "📐 `/th` — случайную теорему (с доказательством)\n"
        "❓ `/q` — случайный вопрос/задачу\n"
        "🎲 `/rand` — совершенно случайный материал любого типа\n"
        "📊 `/stats` — статистику\n"
        "🆘 `/help` — эту справку\n\n"
        "➕ **Короткие команды:** `/d`, `/t`, `/qq`, `/r`\n\n"
        "Удачи в подготовке! 🍀",
        parse_mode="Markdown"
    )

async def random_definition(update: Update, context: ContextTypes.DEFAULT_TYPE):
    if not definitions:
        await update.message.reply_text("❌ Определения не найдены")
        return
    item = random.choice(definitions)
    await update.message.reply_text(
        f"📖 **Определение** (п. {item['number']}):\n\n{item['text']}",
        parse_mode="Markdown"
    )

async def random_theorem(update: Update, context: ContextTypes.DEFAULT_TYPE):
    if not theorems:
        await update.message.reply_text("❌ Теоремы не найдены")
        return
    item = random.choice(theorems)
    await update.message.reply_text(
        f"📐 **Теорема** (п. {item['number']}):\n\n{item['text']}",
        parse_mode="Markdown"
    )

async def random_problem(update: Update, context: ContextTypes.DEFAULT_TYPE):
    if not problems:
        await update.message.reply_text("❌ Вопросы/задачи не найдены")
        return
    item = random.choice(problems)
    await update.message.reply_text(
        f"❓ **Вопрос/Задача** (п. {item['number']}):\n\n{item['text']}",
        parse_mode="Markdown"
    )

async def random_any(update: Update, context: ContextTypes.DEFAULT_TYPE):
    if not all_items:
        await update.message.reply_text("❌ Материалы не найдены")
        return
    item = random.choice(all_items)
    item_type = classify_item(item)
    emoji = get_type_emoji(item_type)
    type_name = get_type_name(item_type)
    
    await update.message.reply_text(
        f"{emoji} **{type_name}** (п. {item['number']}):\n\n{item['text']}",
        parse_mode="Markdown"
    )

async def stats(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "📊 **Статистика базы материалов:**\n\n"
        f"📖 Определений: {len(definitions)}\n"
        f"📐 Теорем: {len(theorems)}\n"
        f"❓ Вопросов/задач: {len(problems)}\n"
        f"━━━━━━━━━━━━━━━━\n"
        f"📚 **Всего материалов:** {len(all_items)}",
        parse_mode="Markdown"
    )

async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "📖 **Доступные команды:**\n\n"
        "📖 `/def` или `/d` — случайное определение\n"
        "📐 `/th` или `/t` — случайная теорема (с доказательством)\n"
        "❓ `/q` или `/qq` — случайный вопрос/задача\n"
        "🎲 `/rand` или `/r` — случайный материал любого типа\n"
        "📊 `/stats` — статистика\n"
        "🆘 `/help` — эта справка\n\n"
        f"📚 **Всего в базе:** {len(all_items)} материалов",
        parse_mode="Markdown"
    )

async def reload(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Перезагружает материалы из PDF"""
    global all_items, definitions, theorems, problems
    
    await update.message.reply_text("🔄 Перезагрузка материалов из PDF...")
    
    raw_text = load_text()
    if not raw_text:
        await update.message.reply_text("❌ Не удалось загрузить PDF-файл")
        return
    
    raw_text = clean_text(raw_text)
    new_items = parse_all_items(raw_text)
    
    if not new_items:
        await update.message.reply_text("❌ Не удалось распарсить материалы из PDF")
        return
    
    all_items = new_items
    definitions = []
    theorems = []
    problems = []
    
    for item in all_items:
        item_type = classify_item(item)
        if item_type == 'definition':
            definitions.append(item)
        elif item_type == 'theorem':
            theorems.append(item)
        else:
            problems.append(item)
    
    await update.message.reply_text(
        f"✅ Материалы перезагружены из PDF!\n\n"
        f"📖 Определений: {len(definitions)}\n"
        f"📐 Теорем: {len(theorems)}\n"
        f"❓ Вопросов/задач: {len(problems)}\n"
        f"📚 Всего: {len(all_items)}"
    )

# ============ ЗАПУСК ============

def main():
    app = Application.builder().token(TOKEN).build()
    
    app.add_handler(CommandHandler("start", start))
    app.add_handler(CommandHandler("def", random_definition))
    app.add_handler(CommandHandler("th", random_theorem))
    app.add_handler(CommandHandler("q", random_problem))
    app.add_handler(CommandHandler("rand", random_any))
    app.add_handler(CommandHandler("stats", stats))
    app.add_handler(CommandHandler("help", help_command))
    app.add_handler(CommandHandler("reload", reload))
    
    app.add_handler(CommandHandler("d", random_definition))
    app.add_handler(CommandHandler("t", random_theorem))
    app.add_handler(CommandHandler("qq", random_problem))
    app.add_handler(CommandHandler("r", random_any))
    
    print("\n🚀 Бот запущен и работает!")
    print("📱 Найдите бота в Telegram и отправьте /start")
    print("⏹️ Для остановки нажмите Ctrl+C\n")
    
    app.run_polling()

if __name__ == "__main__":
    main()
